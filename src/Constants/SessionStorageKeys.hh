<?hh //strict

namespace LayoutCore\Constants;

enum SessionStorageKeys:string as string
{
    DELIVERY_ADDRESS_ID = "deliveryAddressId";
    BILLING_ADDRESS_ID  = "billingAddressId";
    CURRENCY = "currency";
}
